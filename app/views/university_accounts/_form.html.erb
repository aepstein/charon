  <%= form.error_messages %>
  <% form.inputs do %>
  <% unless form.object.new_record? -%>
  <%= form.input( :organization_name, :label => 'Organization',
    :as => :string_with_auto_complete, :remote => {
    :url => organizations_path(:format => :js), :method => :get,
    :with => "'search[name_like]=' + element.value"
  } ) %>
  <% end -%>
  <%= form.input :department_code %>
  <%= form.input :subledger_code %>
  <% end %>
  <%= form.buttons %>

