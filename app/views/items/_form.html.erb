<% fields_for :edition do |edition_form| %>

  <% fields_for :requestable do |requestable_form| %>
    <%= render :partial => "#{form.object.node.requestable_type.tableize}/form",
               :object => requestable_form %>
  <% end %>

  <p>
    <%= edition_form.label :amount,"Requested Amount" %><br />
    <%= edition_form.text_field :amount %>
  </p>

  <p>
    <%= edition_form.label :comment,"Requestor Comment" %><br />
    <%= edition_form.text_field :comment %>
  </p>

<% end %>

