<h1>Listing items</h1>

<table cellspacing="6">
  <tr>
    <th>Item type</th>
    <th colspan="2">Request info</th>
    <th colspan="2">Allocation info</th>
    <th>Available commands</th>
  </tr>

<%= render :partial => 'item',
           :collection => @request.items.root %>

</table>

<p>
New Item:
<select id="node" onchange="new_item_url(this.value)">
<option value="">Select a node</option>
<%= options_from_collection_for_select(
                     @request.basis.structure.nodes.root,
                     :id,
                     :name ) %>
</select>

</p>

