<h2>Editing <%=h @item %></h2>

<%= semantic_form_for(@item, :html => { :multipart => true } ) do |f| %>
  <%= render :partial => 'form', :object => f %>
<% end %>

<p>
<%= link_to 'Show', @item %> |
<%= link_to 'Back', request_items_path(@item.request_id) %>
</p>

