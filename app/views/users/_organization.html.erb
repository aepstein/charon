<%= table_row_tag do %>
  <td><%=h organization.name %></td>
  <td><%=h current_user.roles.in([organization]).join(", ") %></td>
  <td><%= link_to 'Dashboard', dashboard_organization_path(organization) %></td>
<% end %>

