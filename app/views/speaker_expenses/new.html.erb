<h1>New speaker_expense</h1>

<% form_for(@speaker_expense) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :object => f %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', speaker_expenses_path %>

