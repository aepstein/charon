<h1>New basis with <%=h @basis.structure %> structure</h1>

<% form_for( :basis,
             @basis,
             :url => structure_bases_path(@basis.structure) ) do |f| %>

  <%= render :partial => 'form',
             :object => f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', structure_bases_path( @basis.structure ) %>

