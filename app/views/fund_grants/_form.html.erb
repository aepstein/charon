<%= render "shared/error_messages", :target => form.object %>
<%= form.inputs do %>
  <% if form.object.new_record? -%>
  <%= form.input :fund_source,
    :collection => form.object.organization.fund_sources.allowed_for(
      FundEdition::PERSPECTIVES.first, current_user ).open_deadline  %>
  <% end -%>
<% end %>
<%= form.buttons %>

<%
   #TODO -- we should tell the user what frameworks he/she cannot apply for and
   #
%>

