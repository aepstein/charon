  <p>
    Date: <%= local_event_expense.date %>
  </p>

  <p>
    Title: <%= local_event_expense.title %>
  </p>

  <p>
    Location: <%= local_event_expense.location %>
  </p>

  <p>
    Purpose: <%= local_event_expense.purpose %>
  </p>

  <p>
    Anticipated number of attendees: <%= local_event_expense.number_of_attendees %>
  </p>

  <p>
    Admission charge per attendee: <%= number_to_currency(local_event_expense.price_per_attendee) %>
  </p>

  <p>
    Number of publicity copies: <%= local_event_expense.copies_quantity %>
  </p>

  <p>
    Rental equipment, services, and intellectual property use fees: <%= number_to_currency(local_event_expense.services_cost) %>
  </p>

  <p>
    Use of University Property form required? <%= local_event_expense.uup_required? ? 'Yes' : 'No' %>
  </p>

<%= render :partial => "local_event_expenses/calculation", :object => local_event_expense %>

