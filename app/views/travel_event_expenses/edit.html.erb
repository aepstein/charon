<h1>Editing travel_event_expense</h1>

<% form_for(@travel_event_expense) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form',
             :object => f %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @travel_event_expense %> |
<%= link_to 'Back', travel_event_expenses_path %>

