<h2>New fund_source for <%=h @fund_source.organization %></h2>

<%= semantic_form_for( @fund_source, :url => organization_fund_sources_path(@fund_source.organization) ) do |f| %>
  <%= render :partial => 'form', :object => f %>
<% end %>

<p>
<%= link_to 'Back', organization_fund_sources_path( @fund_source.organization ) %>
</p>

