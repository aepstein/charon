<%= form_for(@account_adjustment) do |f| %>
  <% if @account_adjustment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account_adjustment.errors.count, "error") %> prohibited this account_adjustment from being saved:</h2>

      <ul>
      <% @account_adjustment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_transaction %><br />
    <%= f.text_field :account_transaction %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
